# seesaw (redirect only)

이 저장소는 옛 GitHub Pages 주소를 새 도메인으로 넘겨주기 위한 **리다이렉트 전용** 저장소입니다.

- `seesaw-korea.github.io/seesaw/*` → `https://i-eum.org/*`
- 예: `.../seesaw/letter/` → `https://i-eum.org/letter/`

리다이렉트는 `404.html`(모든 경로 처리)과 `index.html`(루트)로 동작합니다.
